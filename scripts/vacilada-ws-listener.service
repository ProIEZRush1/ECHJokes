[Unit]
Description=Vacilada WS Docker event listener
After=docker.service
Requires=docker.service

[Service]
ExecStart=/usr/local/bin/vacilada-ws-docker-listener.sh
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
